import 'package:flutter/material.dart';

class PredictiveModuleScreen extends StatelessWidget {
  const PredictiveModuleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Predictive Module')),
      body: const Center(child: Text('Predictive Module - stub')),
    );
  }
}
